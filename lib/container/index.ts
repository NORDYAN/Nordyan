/**
 * Dependency registration placeholder.
 * Wire concrete repositories, services, and adapters here during feature implementation.
 */
export type ServiceRegistry = Record<string, never>;

export const serviceRegistry: ServiceRegistry = {};
